//Setting up Wifi : 
sudo nano /etc/network/interfaces //run the command in the terminal 



auto lo 

iface lo inet loopback

iface eth0 inet dhcp 

allow-hotplug wlan0

auto wlan0

iface wlan0 inet dhcp        

	wpa-ssid "ssid"

wpa-psk "password"

//substitute the SSID and PSK with your router's name and password. substitute the SSID and PSK with your router's name and password

//Now reboot your pi
sudo reboot

//Instaling PiGPIO library which allows you to control data which you sent through your GPIO pins
sudo apt-get install build-essential unzip wget

wget http://abyz.me.uk/rpi/pigpio/pigpio.zip && unzip pigpio.zip && cd PIGPIO && sudo make install

